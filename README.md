# wireguard_server


# Setup

Setup at least 'SERVERURL' env var in docker-compose.yml

# Run

Easy to run on *nix servers in a docker container

>docker compose up -d


# Clients configs

After start you will find clients' configs to connect in './config' dir
